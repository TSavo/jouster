/**
 * Sample test to demonstrate the issue tracking workflow
 */

describe('Sample Test Suite', () => {
  test('this test will pass', () => {
    expect(1 + 1).toBe(2);
  });

  test('this test will fail', () => {
    expect(1 + 1).toBe(2);
  });
});
